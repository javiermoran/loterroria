import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.loterroria.app',
  appName: 'loterroria',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
